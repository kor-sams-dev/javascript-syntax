const x = 0;
const y = 0;
// const coordinate = { x: x, y: y };
const coordinate = { x, y };
console.log(coordinate);

function makeObj(name, obj) {
  // return {
  //   name: name,
  //   age: age,
  // };
  return {
    name,
    age,
  };
}
