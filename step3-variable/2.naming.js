// 주석 달기

/**
 * 주석읻되며
 * 다음줄로 자동으로 넘어감
 */

/**
 * 변수 규칙!
 * 라틴문자(0-9, a-z, A-Z)
 * 대소문자를 구분함
 * 추천: camelCase (likeThis) ✅
 * 한국어 ❌ (주석정도는 한국어로 사용해도 됨)
 * 예약어 ❌
 * 숫자로 시작 ❌
 * 특수문자 ❌ (_, $ 두글자는 예외)
 * 이모지 ❌
 */

let apple;
let redApple;

// 변수이름은 구체적으로
// bad - 아무런 의미가 없는 변수
let number = 20;
// good - 의미가 있는 변수 이름
let myAge = 20;

// bad - 숫자로 의미없이 늘어나는 변수 이름
let audio1;
let audio2;
// good - 변수마다 의미를 부여함
let backgroundAudio;
let windAudio;

// 꿀팁!
let audioBackground;
let audioWind;
